/*
const http = require("http");

const server = http.createServer((req, res) => {
  //  console.log(req.)
  let rawData=""
  req.on("data", q => rawData+=q)
  req.on("end",()=>{
      console.log(rawData.toString())
  })
});

server.listen(8000);
*/